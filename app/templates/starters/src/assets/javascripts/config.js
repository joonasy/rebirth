/* ========================================
 * Config
 * ======================================== */

import $ from 'jquery';

const config = {
  $document: $(document),
  $window: $(window),
  breakpoint: {
    xs: 320,
    s: 600,
    m: 768,
    ml: 900,
    l: 1024,
    xl: 1200,
  },
};

export default config;
