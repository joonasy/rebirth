production:
  branch: master<% if (typo3) { %>
  exclude: ["<%= appRoot %>/Assets/**/*"]<% } if (wp) { %>
  exclude: ["<%= appRoot %>/assets/**/*"]<% } %>
  host: ""
  include:<% if (typo3) { %>
    "<%= appRoot %>/Resources/Public/**": "Resources/Public"
    "<%= appRoot %>/Resources/Private/Partials/*.dist.html": "Resources/Private/Partials"<% } if (html) { %>
    "dist/**": "dist"<% } if (wp) {  %>
    "<%= appRoot %>/dist/**": "dist"<% } %>
  pass: ""
  path:<% if (wp || typo3) { %>
    local: <%= appRoot %><% } %>
    remote: <% if (html) { %>"your/path"<% } if (wp || typo3) { %>"webroot/ROOT/<%= appRoot %>"<% } %>
  port: 21
  scheme: "ftp"
  slots: 3
  user: ""
